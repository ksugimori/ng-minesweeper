import { MsLongTapDirective } from './ms-long-tap.directive';

describe('MsLongTapDirective', () => {
  it('should create an instance', () => {
    const directive = new MsLongTapDirective();
    expect(directive).toBeTruthy();
  });
});
